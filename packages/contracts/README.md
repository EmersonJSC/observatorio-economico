# Contratos compartilhados

Esta pasta é o local reservado para tipos que precisam ser idênticos na API e
na interface, como `TerritorySummary`, `BudgetSummary` e `ElectionResult`.

Ela ainda não é um pacote publicado: será configurada quando o primeiro
contrato for consumido pelos dois aplicativos. Até lá, não duplicar contratos
novos aqui prematuramente.
