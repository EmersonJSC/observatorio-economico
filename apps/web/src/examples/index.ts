/**
 * Pasta de exemplos — NÃO usada em produção.
 *
 * Cada arquivo aqui é um experimento isolado para estudo de bibliotecas
 * (ex.: deck.gl). Para visualizar um exemplo, importe-o temporariamente
 * em `src/App.tsx` ou crie uma rota dedicada.
 */
export { default as Mapa } from './Mapa'
